# @steganography-js/web

React web UI for embedding and extracting text in PNG files.

## Scripts

- `yarn start` — development server
- `yarn build` — production build
- `yarn test` — run tests
- `yarn deploy` — build and publish with wrangler
